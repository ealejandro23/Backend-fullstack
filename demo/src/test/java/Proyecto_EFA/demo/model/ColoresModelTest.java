package Proyecto_EFA.demo.model;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ColoresModelTest {
    @Test
    void basicConstruction() {
        Colores c = new Colores();
        assertNotNull(c);
    }
}
